{
  "name": "github-repositories",
  "version": "1.0.0",
  "description": "Cypress E2E Tests for the-internet.herokuapp.com",
  "repository": "https://github.com/Ghada-1992/cypress-e2e-tests",
  "main": "index.js",
  "scripts": {
    "cy": "./node_modules/.bin/cypress",
    "test": "npm run cy run -- -q --headed",
    "cy:headless": "npm run cy run -- -q",
    "cy:login": "npm run cy run --spec integration/login.spec.js"
  },
  "author": "Ghada AYARI",
  "license": "ISC",
  "dependencies": {},
  "devDependencies": {
    "cypress": "^5.1.0",
    "cypress-xpath": "^1.6.0"
  }
}
